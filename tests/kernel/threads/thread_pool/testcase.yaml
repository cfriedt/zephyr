common:
  tags: kernel threads
  integration_platforms:
    # - qemu_x86
    # - qemu_x86_nommu
    # - qemu_x86_tiny
    # - qemu_x86_virt
    # - qemu_x86_64
    - qemu_cortex_a53
    - qemu_cortex_a53_smp
    - qemu_cortex_m3
    - qemu_riscv32
    - qemu_riscv32e
    - qemu_riscv64
    - qemu_riscv64_smp
tests:
  kernel.threads.pool.kernel:
    extra_configs:
      - CONFIG_USERSPACE=n
  # kernel.threads.pool.userspace:
  #   tags: userspace
  #   extra_configs:
  #     - CONFIG_USERSPACE=y
